# 🐷 小猪猪教育数字资源库 - 使用说明

## 🚀 快速开始（3分钟部署到 GitHub Pages）

### 第一步：Fork 或上传代码

1. 在 GitHub 上创建新仓库（如 `piggy-resource-library`）
2. 将所有文件上传到仓库
3. 仓库结构：
```
├── index.html          # 学生端入口
├── admin.html           # 管理后台入口
├── share.html           # 公开资源分享页
├── assets/
│   ├── style.css        # 全局样式
│   ├── config.js        # 配置文件
│   ├── init.js          # 初始化脚本
│   ├── storage.js       # 存储层（GitHub API + LocalStorage）
│   ├── github-api.js    # GitHub API 封装
│   ├── auth.js          # 认证模块
│   ├── app.js           # 学生端主逻辑
│   ├── admin.js         # 管理后台逻辑
│   └── share.js         # 分享页面逻辑
├── api/
│   └── oauth.js         # OAuth 代理（可选）
└── .github/
    └── workflows/
        └── deploy.yml    # 自动部署
```

### 第二步：开启 GitHub Pages

1. 进入仓库 **Settings → Pages**
2. **Source** 选择 `Deploy from a branch`
3. **Branch** 选择 `main` → `/ (root)`
4. 点击 Save，等待约 1 分钟
5. 访问 `https://你的用户名.github.io/仓库名/` 即可

### 第三步：开始使用

**演示账号（无需注册即可体验）：**
- `admin_user` / `123456` — 旗舰版用户
- `demo_student` / `123456` — 专业版用户
- `test_user` / `123456` — 免费版（预览权限关闭）

**管理后台：** 访问 `admin.html`，密码 `123456`

---

## 🔐 GitHub 登录配置（可选）

### 方式一：Personal Access Token（推荐快速开始）

1. GitHub → **Settings → Developer settings → Personal access tokens → Tokens (classic)**
2. 点击 **Generate new token (classic)**
3. 勾选权限：`repo`（完整仓库访问）
4. 生成后复制 token
5. 在管理后台 → **GitHub Token** 中粘贴保存

### 方式二：GitHub OAuth App（完整 OAuth 体验）

1. GitHub → **Settings → Developer settings → OAuth Apps → New OAuth App**
2. 填写信息：
   - **Application name**: 小猪猪教育数字资源库
   - **Homepage URL**: `https://你的域名`
   - **Callback URL**: `https://你的域名`（或使用 Cloudflare/Vercel 代理）
3. 获取 **Client ID** 和 **Client Secret**
4. 编辑 `assets/config.js`，修改：
```javascript
GITHUB: {
    clientId: '你的ClientID',  // 替换为实际值
    ...
}
```
5. 部署 OAuth 代理（参考 `api/oauth.js` 注释）

---

## 📊 核心功能一览

### 学生端 (`index.html`)
| 功能模块 | 说明 |
|---------|------|
| ✅ GitHub 登录 | 一键登录，无需注册 |
| ✅ 账号密码登录 | 传统登录方式 |
| ✅ 注册/找回密码 | 手机号短信验证（演示模式自动填充） |
| ✅ 文件浏览 | 仓库文件 + Release 附件 |
| ✅ 在线查看 | 视频/图片/PDF 预览 + 短信验证 |
| ✅ 下载管理 | 秒点计费 + 批量下载队列 |
| ✅ AI 问答 | 截图提问 + 语音提问 |
| ✅ 购买中心 | 会员/秒点/卡券 三 Tab |
| ✅ 订单管理 | 提交/查看/退款 |
| ✅ 邀请返利 | 邀请码 + 奖励机制 |
| ✅ 学习进度 | 时长追踪 + 完成度 |
| ✅ AI 推荐 | 基于收藏/下载历史 |
| ✅ 收藏标签 | 自定义标签分类 |
| ✅ 离线缓存 | IndexedDB 本地存储 |
| ✅ 学习小组 | 创建/加入/共享 |
| ✅ 积分任务 | 签到/分享/评价 |
| ✅ 资源评价 | 1-5星 + 文字评论 |
| ✅ 全文检索 | 文件名/标签/描述 |
| ✅ 消息通知 | 订单/公告/系统 |
| ✅ 夜间模式 | 深色主题 + 字体调节 |
| ✅ 多端书签 | 跨设备同步 |
| ✅ 数据看板 | 学习数据可视化 |
| ✅ 自动续费 | 到期自动续期 |

### 管理后台 (`admin.html`)
| 功能模块 | 说明 |
|---------|------|
| ✅ Token 配置 | GitHub API 认证 |
| ✅ 文件管理 | CRUD + Release 上传 |
| ✅ Issue 管理 | 创建/编辑/关闭 |
| ✅ 用户权限 | 登录/预览/下载 三权限 |
| ✅ 套餐管理 | 会员 + 秒点包 |
| ✅ 优惠券 | 折扣/有效期/次数 |
| ✅ 订单审批 | 通过/拒绝 + 自动处理 |
| ✅ 退款审批 | 批准/拒绝 + 额度回退 |
| ✅ 公告管理 | 富文本 + 排序 |
| ✅ 流量统计 | 下载次数/流量 |
| ✅ 访问统计 | UV/PV 按日 |

### 公开分享 (`share.html`)
- 无需登录
- 浏览 Every-day-Share 仓库
- 文件预览/下载
- 公告展示

---

## 🗄️ 数据存储架构

### 双模式存储

```
┌─────────────────────────────────────────────────────┐
│                     Storage 层                       │
├─────────────────────┬───────────────────────────────┤
│   GitHub 模式       │   LocalStorage 模式（默认）    │
│   (生产环境)        │   (演示/开发环境)             │
│                     │                               │
│  GitHub Repository  │   Browser LocalStorage        │
│  ├── _data/*.json   │   ├── piggy__data/users.json │
│  └── 文件/Release   │   ├── piggy__data/orders.json│
│                     │   └── ...                     │
│  通过 GitHub API    │   通过 localStorage API       │
│  REST API 读写      │   同步读写                    │
└─────────────────────┴───────────────────────────────┘
```

### 切换模式

在管理后台配置 GitHub Token 后自动切换到 GitHub 模式，
所有数据读写自动路由到 GitHub Repository 的 `_data/` 目录。

---

## ⚙️ 配置说明

### 修改 `assets/config.js`

```javascript
const CONFIG = {
    GITHUB: {
        owner: 'YOUR_USERNAME',       // ← 你的 GitHub 用户名
        mainRepo: 'resource-main',     // ← 主仓库名
        shareRepo: 'Every-day-Share',  // ← 分享仓库名
        branch: 'main',
        clientId: 'YOUR_CLIENT_ID',    // ← OAuth Client ID
        redirectUri: window.location.origin + '/',
    },
    ADMIN_PASSWORD: '123456',          // ← 管理端密码
    // ...
};
```

### 环境变量（GitHub Actions）

在仓库 **Settings → Secrets and variables → Actions** 中添加：
- `CLIENT_ID`: OAuth App Client ID
- `CLIENT_SECRET`: OAuth App Client Secret

---

## 🔄 工作流程

### 管理员上传文件流程
```
1. 登录管理后台 (admin.html + 密码123456)
2. 配置 GitHub Token
3. 进入「主仓库文件」
4. 点击「上传文件」→ 选择 Release → 上传
5. 文件自动作为 Release Asset 存储
6. 学生端首页自动展示
```

### 学生购买流程
```
1. 注册/登录 (GitHub 或 账号密码)
2. 浏览资源 → 点击「在线查看」/「下载」
3. 进入购买中心 → 选择套餐/秒点包
4. 输入优惠码（可选）→ 上传付款凭证
5. 提交订单 → 等待管理员审批
6. 审批通过后自动升级/增加秒点
```

---

## 🛠️ 自定义开发

### 添加新功能模块
1. 在 `index.html` 添加页面 HTML
2. 在 `assets/app.js` 添加逻辑函数
3. 在导航菜单添加入口

### 对接真实 AI 服务
修改 `assets/app.js` 中的 `screenshotQuestion()` 和 `voiceQuestion()`：
```javascript
// 替换为实际 API 调用
async function screenshotQuestion() {
    const response = await fetch('你的AI服务URL', {
        method: 'POST',
        body: JSON.stringify({ image, question })
    });
    // 处理响应...
}
```

### 对接真实短信服务
修改 `assets/auth.js` 中的 `sendCaptcha()`：
```javascript
sendCaptcha(phone) {
    // 调用短信服务 API
    fetch('你的短信服务URL', {
        method: 'POST',
        body: JSON.stringify({ phone, code: this.generateCode() })
    });
}
```

---

## 📝 注意事项

1. **GitHub API 速率限制**：未认证 60次/小时，认证后 5000次/小时
2. **文件大小限制**：GitHub 单文件最大 100MB（建议大文件使用 Git LFS）
3. **静态托管限制**：GitHub Pages 不支持后端代码，OAuth 需额外服务
4. **演示模式数据**：LocalStorage 数据在清除浏览器数据后丢失
5. **CORS 问题**：直接调用 GitHub API 可能遇到 CORS，建议通过 jsDelivr CDN 访问文件

---

## 📄 License

MIT License - 自由使用、修改、分发
